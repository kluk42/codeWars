function maxSequence (arr) {
  if (arr.length === 0) return 0;
  let maxSum;
  let startingIndex;
  let finalIndex;
  let arrToChange = Object.assign([], arr);
  while (maxSum >= 0) {
    sum.push(sum[sum.length-1]+)
    sum += arr[i]
  }
}

console.log(maxSequence([-2,1,-3,4,-1,2,1,-5,4]));

[1, 5, -5, 1, 4, 1, -1, 1, 1, 1, 1, 1]